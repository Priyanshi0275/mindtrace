# In Vercel: set this in Project Settings -> Environment Variables to the
# public URL of wherever your Django backend is deployed (Render/Oracle/Fly).
# It CANNOT point at localhost once deployed.
NEXT_PUBLIC_API_URL=http://localhost:8000
